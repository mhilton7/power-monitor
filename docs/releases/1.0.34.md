# Power Monitor Server 1.0.34

## Release identity

- Server version: `1.0.34`
- Alembic head: `20260803_0030`
- Device protocol: `pm-protocol/1.0.0` (unchanged)

Version 1.0.34 supersedes the image-free 1.0.33 publication attempt. Earlier
failure tags remain immutable and are never reused.

## Production-target dependency evidence

- Generates the backend vulnerability audit and CycloneDX SBOM inside the
  pinned `python:3.13.5-slim-bookworm` linux/amd64 environment used by the API
  image instead of inheriting the release host platform.
- Evaluates requirement markers against one explicit CPython 3.13.5,
  Linux/x86-64 environment on Windows and Linux verifiers alike.
- Adds regression coverage proving Windows-only packages are excluded and
  Linux runtime packages are required.
- Retains canonical UTF-8-without-BOM/LF artifacts, complete source-history
  provenance, and every existing fail-closed release gate.

## TrueNAS deployment

The production target remains linux/amd64 with only Caddy publishing TCP 8443.
PostgreSQL remains mounted at `/mnt/Apps/Power/postgres`.
