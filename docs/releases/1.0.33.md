# Power Monitor Server 1.0.33

## Release identity

- Server version: `1.0.33`
- Alembic head: `20260803_0030`
- Device protocol: `pm-protocol/1.0.0` (unchanged)

Version 1.0.33 supersedes the image-free 1.0.32 publication attempt. Both
earlier failure tags remain immutable and are not reused.

## Publication reliability

- Retains the cross-platform UTF-8-without-BOM/LF evidence repair from 1.0.32.
- Fetches full Git history for source validation so the verifier can read and
  hash every exact blob from the recorded tested source commit.
- Adds a workflow regression proving the source-validation job cannot regress
  to a shallow checkout.
- Preserves fail-closed provenance, migration, dependency-audit, protocol,
  contract, Compose, frontend, PostgreSQL load, image-build, and immutable-tag
  gates.

## TrueNAS deployment

Production remains linux/amd64, digest-pinned, and Caddy-only on the host. The
PostgreSQL bind mount remains exactly:

```yaml
volumes:
  - /mnt/Apps/Power/postgres:/var/lib/postgresql/data
```
